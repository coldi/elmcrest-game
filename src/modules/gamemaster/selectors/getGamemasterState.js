/**
 * Selects the gamemaster state.
 * @param {Object} state The global state
 * @returns {Object} The gamemaster state
 */
const getGamemasterState = (state) => state.gamemaster;

export default getGamemasterState;
