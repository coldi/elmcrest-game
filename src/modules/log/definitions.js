import { DEFAULT_MESSAGE_TYPE } from './constants';

/**
 * The message default properties.
 * @type {Object}
 */
export const messageDefaults = { // eslint-disable-line
    id: '',
    type: DEFAULT_MESSAGE_TYPE,
    text: '',
    params: null,
};
