/**
 * Selects the procedural state.
 * @param {Object} state The global state
 * @returns {Object} The procedural state
 */
const getProceduralState = (state) => state.procedural;

export default getProceduralState;
