/**
 * Selects the effects state.
 * @param {Object} state The global state
 * @returns {Object} The effects state
 */
const getEffectsState = state => state.effects;

export default getEffectsState;
