/**
 * Selects the characters state.
 * @param {Object} state The global state
 * @returns {Object} The characters state
 */
const getCharactersState = state => state.characters;

export default getCharactersState;
