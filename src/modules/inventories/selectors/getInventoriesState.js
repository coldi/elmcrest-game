/**
 * Selects the inventories state.
 * @param {Object} state The global state
 * @returns {Object} The inventories state
 */
const getInventoriesState = state => state.inventories;

export default getInventoriesState;
