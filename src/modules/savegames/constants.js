/**
 * The default key name used for local storage.
 * @type {string}
 */
export const SAVED_STATE_KEY = 'gameState';

/**
 * The delay in ms how long it should be waited
 * before performing an (debounced) auto-save.
 * @type {number}
 */
export const AUTO_SAVE_DELAY = 500;
